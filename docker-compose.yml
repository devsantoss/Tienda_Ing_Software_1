version: "3.9"

services:
  
  basededatos:
    build: .    
    stdin_open: true
    tty: true
    volumes:
      - db:/app/db
  
  sebastian:
    build: 
      context: /Sebastian
      dockerfile: /Dockerfile 
    volumes:      
      - db:/app/db

  reinaldo:
    build: 
      context: /Reinaldo
      dockerfile: /Dockerfile 
    volumes:      
      - db:/app/db

  helena:
    build: 
      context: /Helena
      dockerfile: /Dockerfile 
    volumes:      
      - db:/app/db

  sonia:
    build: 
      context: /Sonia
      dockerfile: /Dockerfile 
    volumes:      
      - db:/app/db
    

  api-gateway:
    image: nginx:alpine
    volumes:
      - "./nginx.conf:/etc/nginx/nginx.conf:ro"
    command: [nginx-debug, '-g', 'daemon off;']
    links:
      - sebastian
      - reinaldo
      - helena
      - sonia
    ports:
      - "5000:80"
volumes:
  db: